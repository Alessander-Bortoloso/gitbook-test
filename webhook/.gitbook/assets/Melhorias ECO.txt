- Possibilitar ATUALIZAÇÃO de informações variáveis dos eventos (servicebus, fila, schema e descrição)
- Possibilitar ATUALIZAÇÃO das assinaturas de eventos (fila e servicebus).
- Forçar a atualização dos sinks quando uma configuração é atualizada. (Atualmente só atualiza se remover a assinatura, aguardar a atualização do ECO e recriar novamente para que na próxima atualização seja refletido)
- Ter uma API para gerenciamento do ECO (Migrar as operações existentes no Catalogo de APIs para uma API do ECO)
- Revisar processo de assinatura de eventos privados (regras de solicitação)
- Busca simplificada da connection string do service bus dos clientes (publisher/consumers. Secret ref com KV).
- Possibilitar a conversa ECO <-> ECO, de forma que um ECO possa se conectar em "n" eventhubs. (Pode resolver o problema de multiplos pvt endpoints) - Precisa pensar bem como (Obs: múltiplos eventhubs por namespace)
- Disponibilizar MCP para consultas de informações do ECO (Assinaturas por evento, Assinaturas por produto)
- Status de conectividade/listagem de cada servicebus conectado ao ECO.
- Listagem de tópicos de eventhub por namespace (Localizar facilmente uma "label" de um evento)
- Incluir funcionalidade que cria as configurações de timestamp quando não existirem para uma determinada label (útil para nova implantações, atualmente precisa criar o timestamp manualmente)
- Verificação detalhada do status de erro de conexão para erros conhecidos (fila inexistente, unauthorized, timeout de conexão - pvt endpoint, sem espaço no eventhub)
- Conexão com servisse bus dos clientes por meio do Managed Identity (Verificar a possibilidade)
  > https://learn.microsoft.com/en-us/azure/service-bus-messaging/service-bus-managed-service-identity?context=%2Fazure%2Factive-directory%2Fmanaged-identities-azure-resources%2Fcontext%2Fmsi-context
- Implementar regra para validar para que uma fila de destino não exista já como uma fila de origem, para evitar um loop (Já tivemos casos assim)


docker run -e "ACCEPT_EULA=Y" -e "SA_PASSWORD=MinhaSenhaForte123!" -p 1433:1433 --name internationalization -v sqlserver_data:/var/opt/mssql mcr.microsoft.com/mssql/server:2022-latest

Doc notificação X
Python X
Feedback Michael X
Extrair Token Service 
Implantar Token Service nas libs de notificação